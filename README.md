# Python API with Automated Deployment and Teardown

This project implements a FastAPI application with automated infrastructure provisioning using Terraform.

## Project Structure

- `app/` - FastAPI application code
- `terraform/` - Terraform configuration files
- `Dockerfile` - Docker container definition
- `requirements.txt` - Python dependencies

## Setup

See design_review.md for detailed implementation plan.

